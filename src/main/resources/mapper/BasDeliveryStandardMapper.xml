<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hongqi.springboot.dao.BasDeliveryStandardDao">

    <select id="findStandards" resultType="com.hongqi.springboot.model.BasDeliveryStandard">
       select b.id,b.name,b.minweight,b.maxweight,b.operationtime,b.invalidateSign,e.empname,u.name as uname from BAS_DeliveryStandard b INNER JOIN sy_emp e on b.OperatorID = e.ID INNER JOIN sy_units u on b.OperationUnitID = u.ID
    </select>

</mapper>